#  $Id: 00index.txt,v 1.49 2010/10/22 09:18:15 choutko Exp $

Makefile //  General Makefile
Makefile.linux    // OS dep -> Linux-gcc
Makefile.linuxicc // OS dep -> Linux-icc
Makefile.Linux.24 // OS dep -> Linux-OLD icc --> Special arch enabled maually  by: setenv  ARCH Linux.24 
Makefile.OSf1     //  OS dep ( to be updated)
          // setenv   DEBUGFLAG 1      sets the -g option in compiling; 
	  // unsetenv DEBUGFLAG        turn on  optimizatio in compiling;
          // setenv   AMSPROFILE 1     adds gprof;
	  // setenv   AMSDEBUG         enable the debug code into the GBATCH source
	  // setenv   AMSP 1           enable the parallel processing (Intel compilers only)
          // setenv   PGTRACK 1        enable the new tracker SW
	  
Moptg4    // makefile to gmake g4 batch ! Only RH7.3 is supported !!!
          // setenv AMSDEBUG 1 debug version; unsetenv AMSDEBUG opt version
          // setenv O4 1   super-opt option (alpha only)
          // setenv AMSWD to some tmp dir is  recommended
Moptg4i    // makefile to gmake geant interactive  g4



Overall:

The choice of the compiler suite (gcc/g77  or icc/ifort) is automaticaly done
as a consquence of the root-config --arch command.
It then depends on the compiler type has been used for  your ROOT version. 

The new Perugia Tracker Software can be enabled at compile time with:
setenv PGTRACK 1

WARNING!!: Perugia Tracker Software is under developpement, 
         and it is not yet full featured.

gmake gbatch(default) // builds g3 gbatch 
gmake gxint           // builds g3 gxint 
gmake lib             // builds root shared & static library
gmake shared          // builds root shared library
gmake static          // builds root static library
gmake producer        // builds gbatch-orbit (add -i flag for Intel Compilers mode)
gmake server          // builds server       (add -i flag for Intel Compilers mode)

gmake all             //  build gbatch,gxint,lib,producer,display ,offmon
build_all            //   build everything
gmake -f Moptg4   //builds g4 batch (not supported at the moment)
gmake -f Moptg4i  //builds g4 interactive (not supported at the moment)




NB icc compiler 11 

doesn't work well with 2.4 kernels and/or  2002/2003 geant3 libraries compiled with icc7.

therefore for 2.4 and/or mc related jobs using cernlib 2002/2003 :

(on slc4 only, not slc5)  : ex:  from pcamsj1.cern.ch ssh 192.168.50.133
source /Offline/vdev/install/amsvar.slc4icc
setenv ARCH Linux.24
gmake -j 2

The executable  will be put in $AMSWD/exe/Linux.24/


